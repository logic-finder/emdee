#ifndef MARKDOWN_H
#define MARKDOWN_H

/**
 * @brief `translate_markdown` reads `line` and translates it on the Markdown syntax.
 * @param line a string to translate
 * @return a translated line
 */
char *translate_markdown(const char *line);

#endif