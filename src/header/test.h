#ifndef TEST_H
#define TEST_H

/**
 * @brief `perform_test` reads testcases from a text file and give them tests.
 * @param filename the name of a text file consisting of testcases
 */
int perform_test(const char *filename);

#endif